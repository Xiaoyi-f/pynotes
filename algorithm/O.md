O(n):
    1.执行固定次数,n是明确值
    2.序列值/不明确值,一个for
O(n**2): 双层for循环
O(log2(n)): for (int i = 1; i <= n; i *= 2)
O(n*m):     
    for i in range(n):
        for j in range(m):
            pass 
O(n+m): forn form 两个for不嵌套
O(n!): 阶乘运算模型

字典序: 
    数字由小向大
    字母先大写A-Z再小写a-z


